# Mask use flags on specific packages / versions

# Xarblu <xarblu@protonmail.com> (2025-02-19)
# For the RCs only the main "cachyos" flavour is supported
# so mask all the others
>=sys-kernel/cachyos-kernel-6.14 bmq bore deckify eevdf hardened rt-bore server

# Xarblu <xarblu@protonmail.com> (2025-01-20)
# Missing patches
>=sys-kernel/cachyos-kernel-6.13.0 hardened

# Xarblu <xarblu@protonmail.com> (2025-01-10)
# Incompatible/broken hardened patch
>=sys-kernel/cachyos-kernel-6.12.9 hardened
